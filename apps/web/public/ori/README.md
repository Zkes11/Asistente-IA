Coloca aquí los assets oficiales de Ori (Oribot) con estos nombres exactos:

- `ori-greeting.png`
- `ori-thinking.png`
- `ori-analyzing.png`
- `ori-celebrating.png`

La UI ya referencia esas rutas públicas (`/ori/...`). Si los PNG no existen, el componente `OriAvatar` muestra un fallback visual temporal para evitar romper la interfaz.
